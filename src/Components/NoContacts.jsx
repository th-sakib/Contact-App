

const NoContacts = ({ className }) => {
    return (
        <div className="text-black flex justify-center items-center h-[50vh] flex-col font-bold text-3xl font-Protest-Guerrilla">
        	<img src="noContact.png" alt="no contact" />
        	<h1>No contacts!</h1>
        </div>
    );
};

export default NoContacts;
